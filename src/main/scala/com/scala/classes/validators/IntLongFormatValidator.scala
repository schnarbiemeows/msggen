/*
 * Created 2019 by Dylan Kessler
 */

package com.scala.classes.validators

/**
  * class used for validating the formats specified for the
  * Int and Long data types
  */
object IntLongFormatValidator {

  /**
    *
    * @param format = format specified
    * @return (isValidated:Boolean,message:String)
    */
  def validateEnumIntFormat(format:String):Tuple2[Boolean,String] = {
    var isValidated = true
    var message:String = "NONE"
    // TODO - finish
    (isValidated,message)
  }

  /**
    *
    * @param format = format specified
    * @return (isValidated:Boolean,message:String)
    */
  def validateEnumLongFormat(format:String):Tuple2[Boolean,String] = {
    var isValidated = true
    var message:String = "NONE"
    // TODO - finish
    (isValidated,message)
  }


  /**
    *
    * @param format = format specified
    * @return (isValidated:Boolean,message:String)
    */
  def validateRandomIntFormat(format:String):Tuple2[Boolean,String] = {
    var isValidated = true
    var message:String = "NONE"
    // TODO - finish
    (isValidated,message)
  }

  /**
    *
    * @param format = format specified
    * @return (isValidated:Boolean,message:String)
    */
  def validateRandomLongFormat(format:String):Tuple2[Boolean,String] = {
    var isValidated = true
    var message:String = "NONE"
    // TODO - finish
    (isValidated,message)
  }

  /**
    *
    * @param format = format specified
    * @return (isValidated:Boolean,message:String)
    */
  def validateExternalIntFormat(format:String):Tuple2[Boolean,String] = {
    var isValidated = true
    var message:String = "NONE"
    // TODO - finish
    (isValidated,message)
  }

  /**
    *
    * @param format = format specified
    * @return (isValidated:Boolean,message:String)
    */
  def validateExternalLongFormat(format:String):Tuple2[Boolean,String] = {
    var isValidated = true
    var message:String = "NONE"
    // TODO - finish
    (isValidated,message)
  }

  /**
    *
    * @param format = format specified
    * @return (isValidated:Boolean,message:String)
    */
  def validateRangeIntFormat(format:String):Tuple2[Boolean,String] = {
    var isValidated = true
    var message:String = "NONE"
    // TODO - finish
    (isValidated,message)
  }

  /**
    *
    * @param format = format specified
    * @return (isValidated:Boolean,message:String)
    */
  def validateRangeLongFormat(format:String):Tuple2[Boolean,String] = {
    var isValidated = true
    var message:String = "NONE"
    // TODO - finish
    (isValidated,message)
  }
}
