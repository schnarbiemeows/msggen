/*
 * Created 2019 by Dylan Kessler
 */

package com.scala.classes.validators

/**
  * class used for validating the formats sepcified for the Float
  * and Double data types
  */
object FloatDoubleFormatValidator {

  /**
    *
    * @param format = format specified
    * @return (isValidated:Boolean,message:String)
    */
  def validateEnumFloatFormat(format:String):Tuple2[Boolean,String] = {
    var isValidated = true
    var message:String = "NONE"
    // TODO - finish
    (isValidated,message)
  }

  /**
    *
    * @param format = format specified
    * @return (isValidated:Boolean,message:String)
    */
  def validateEnumDoubleFormat(format:String):Tuple2[Boolean,String] = {
    var isValidated = true
    var message:String = "NONE"
    // TODO - finish
    (isValidated,message)
  }


  /**
    *
    * @param format = format specified
    * @return (isValidated:Boolean,message:String)
    */
  def validateRandomFloatFormat(format:String):Tuple2[Boolean,String] = {
    var isValidated = true
    var message:String = "NONE"
    // TODO - finish
    (isValidated,message)
  }

  /**
    *
    * @param format = format specified
    * @return (isValidated:Boolean,message:String)
    */
  def validateRandomDoubleFormat(format:String):Tuple2[Boolean,String] = {
    var isValidated = true
    var message:String = "NONE"
    // TODO - finish
    (isValidated,message)
  }

  /**
    *
    * @param format = format specified
    * @return (isValidated:Boolean,message:String)
    */
  def validateExternalFloatFormat(format:String):Tuple2[Boolean,String] = {
    var isValidated = true
    var message:String = "NONE"
    // TODO - finish
    (isValidated,message)
  }

  /**
    *
    * @param format = format specified
    * @return (isValidated:Boolean,message:String)
    */
  def validateExternalDoubleFormat(format:String):Tuple2[Boolean,String] = {
    var isValidated = true
    var message:String = "NONE"
    // TODO - finish
    (isValidated,message)
  }

  /**
    *
    * @param format = format specified
    * @return (isValidated:Boolean,message:String)
    */
  def validateRangeFloatFormat(format:String):Tuple2[Boolean,String] = {
    var isValidated = true
    var message:String = "NONE"
    // TODO - finish
    (isValidated,message)
  }

  /**
    *
    * @param format = format specified
    * @return (isValidated:Boolean,message:String)
    */
  def validateRangeDoubleFormat(format:String):Tuple2[Boolean,String] = {
    var isValidated = true
    var message:String = "NONE"
    // TODO - finish
    (isValidated,message)
  }
}
